# PostgreSQL support (optional — install manually when DB_ENGINE=postgresql)
# psycopg2-binary>=2.9
# MySQL / MariaDB support (optional — install manually when DB_ENGINE=mysql or mariadb)
# mysqlclient>=2.2
# Oracle support (optional — install manually when DB_ENGINE=oracle)
# oracledb>=4.0
# SBM community strategy (optional — required only for --community-strategies SBM).
# graph-tool is NOT installable from pip: use conda-forge ("conda install -c conda-forge graph-tool")
# or system packages (Debian/Ubuntu: apt install python3-graph-tool, then create the venv with
# --system-site-packages). See docs/community-detection.md.
# graph-tool
djangorestframework==3.17.1
fa2==1.1.2
django==6.0.6
django-admin-logs==1.5.0
django-colorfield==0.14.0
django-extensions==4.1
python-decouple==3.8
tomlkit>=0.13
rich>=13  # progress bars + summary tables for export_installation.py (imported optionally; plain fallback if absent)
telethon==1.43.2
pypalettes==0.2.1
colorcet==3.2.1
leidenalg==0.12.0
openpyxl==3.1.5
umap-learn==0.5.12
pandas==3.0.3
networkx==3.6.1
# numpy is otherwise a transitive dependency (pandas/scipy/networkx), but is pinned below 2.0 for
# ABI compatibility with apt/conda graph-tool builds compiled against numpy 1.x (see the graph-tool
# note above): a 1.x-compiled extension cannot load under numpy 2.x. Floor 1.26 = pandas 3.0's minimum.
numpy>=1.26,<2
